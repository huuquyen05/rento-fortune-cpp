#include <bits/stdc++.h>
#include "communityChest.h"
#include "player.h"

CommunityChest::CommunityChest(std::string type,int amount,std::string sentence,int steps,int location)
    : type(type),amount(amount),sentence(sentence),steps(steps),location(location) {}

void CommunityChest::trigger(Player *player){
    if(type=="change"){
        
    }
    if(type=="advanceTo"){

    }
    if(type=="changeEach"){

    }
    if(type=="outOfJail"){

    }
    if(type=="goToJail"){

    }
    if(type=="repair"){

    }
}


